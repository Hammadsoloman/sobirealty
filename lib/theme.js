/* theme.ts */
import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    brand: {
      primary: "#0F52BA",
      secondary: "#DA00EC",
      black: "#000000",
    },
  },
// fonts: {
//   heading: "var(--font-brand)",
//   body: "var(--font-brand)",
// },
});
