/* theme.ts */
import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    brand: {
      primary: "#DA00EC",
      secondary: "#0F52BA",
      black: "#000000",
    },
  },
// fonts: {
//   heading: "var(--font-brand)",
//   body: "var(--font-brand)",
// },
});
